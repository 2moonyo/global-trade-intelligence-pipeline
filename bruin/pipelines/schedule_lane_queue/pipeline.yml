name: schedule_lane_queue
start_date: "2026-04-11"
catchup: false
retries: 0
tags: ["ops", "queue", "orchestration"]
domains: ["orchestration"]
meta:
  retry_owner: ops_ledger
  orchestration_source: bruin
variables:
  schedule_lane:
    type: string
    default: "weekly_refresh"
  batch_plan_path:
    type: string
    default: "ops/batch_plan.json"
